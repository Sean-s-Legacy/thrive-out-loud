import React from 'react'
import { Button, Space, Input } from 'antd';
import 'antd/dist/reset.css';

export default function SignupBtn() {
  return (
    <Button  type="text" className='common_btn auth_btn primary_btn'> 
    Sign up
   </Button>
  )
}
